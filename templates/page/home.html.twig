{% extends 'base.html.twig' %}

{% block title %}Corono Musical - Accueil{% endblock %}

{% block body %}

    <main>

        <header class="home-header">
            <div class="container home-header__container">
                <h1>Bienvenue sur Corona Musical !</h1>
                {% if app.user %}
                    <button class="btn"><a href="{{ path('albums') }}">Découvrir</a></button>
                {% else %}
                    <div>
                        <button class="btn"><a href="{{ path('app_register') }}">S'inscire</a></button>
                        <button class="btn"><a href="{{ path('app_login') }}">Se connecter</a></button>
                    </div>
                {% endif %}
            </div>
        </header>

    </main>

{% endblock %}